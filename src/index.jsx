// b1 them cac thu vien
import React from "react";
import ReactDOM from "react-dom";
import App from "./App"
;// b2 them the div co ID
const el = document.getElementById("root");
//b3 Them react dieu khien the root
const root = ReactDOM.createRoot(el);
//b4 

// b5
root.render(<App />);
